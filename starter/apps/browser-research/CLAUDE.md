# Browser Research App

This starter is optimized for web investigation, live browsing, and evidence-backed summaries.

## Preferred Behavior

- Prefer `web_site_context` before browser interaction when the target site is known.
- Use browser tools when static fetching is incomplete or the task requires a real browser session.
- Close tabs you open when the task is complete.
